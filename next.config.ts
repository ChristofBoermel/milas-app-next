import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Custom i18n implementation via LanguageContext
  // Supported languages: en (English), de (German), pl (Polish)
};

export default nextConfig;
